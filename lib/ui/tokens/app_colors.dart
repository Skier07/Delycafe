import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(2, 141, 255, 1);
  static const onPrimary = Color.fromRGBO(252, 252, 252, 0.8);
  static const header = Color.fromRGBO(12, 32, 77, 1);
  static const bannerOverlay = Color.fromRGBO(0, 0, 0, 0.6);
  static const buttonText = Colors.white;
}
