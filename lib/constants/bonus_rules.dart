/// Default bonus rules when the API does not return percentages.
class BonusRules {
  static const int earnPercent = 5;
  static const int maxSpendPercent = 30;
  static const int firstOrderDiscountPercent = 20;
}
